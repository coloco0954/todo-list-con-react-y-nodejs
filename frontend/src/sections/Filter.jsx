const Filter = ({ searchValue, onChange }) => <input type="text" name="" id="" value={searchValue} onChange={onChange} placeholder="buscar..." className="w-[300px] rounded-md px-2 focus:outline-none" />

export default Filter